export enum CoachVerificationStatus {
  PENDING = "pending",
  UNDER_REVIEW = "under_review",
  VERIFIED = "verified",
  REJECTED = "rejected",
  EXPIRED = "expired"
}

export enum CoachAccountStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
  SUSPENDED = "suspended"
}

export enum CoachEligibility {
  ELIGIBLE = "eligible",
  INELIGIBLE = "ineligible"
}

export enum CoachProfileVisibility {
  PUBLIC = "public",
  PRIVATE = "private"
}
